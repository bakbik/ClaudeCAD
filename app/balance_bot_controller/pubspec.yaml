name: balance_bot_controller
description: Smartphone controller for self-balancing robot via Bluetooth Classic

publish_to: "none"

environment:
  sdk: flutter

dependencies:
  flutter:
    sdk: flutter
  flutter_bluetooth_serial: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
